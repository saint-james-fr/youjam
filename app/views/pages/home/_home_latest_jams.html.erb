<div class="background-latest-jam" style="background-image: linear-gradient(180deg, #ddd9e6 50%, rgba(41,34,162,0.5) 100%)">
  <div class="container mt-5 mb-2">
    <h2>Les Jams les plus populaires</h2>
    <div class="jam-cards mt-4">
      <% @jams.first(4).each do |jam| %>
        <%= render "shared/jam_card", jam: jam %>
      <% end %>
    </div>
  </div>
</div>
